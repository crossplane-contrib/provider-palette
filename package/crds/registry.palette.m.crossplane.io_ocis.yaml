---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ocis.registry.palette.m.crossplane.io
spec:
  group: registry.palette.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - palette
    kind: Oci
    listKind: OciList
    plural: ocis
    singular: oci
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Oci is the Schema for the Ocis API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OciSpec defines the desired state of Oci
            properties:
              forProvider:
                properties:
                  baseContentPath:
                    description: |-
                      (String) The relative path to the endpoint specified.
                      The relative path to the endpoint specified.
                    type: string
                  credentials:
                    description: |-
                      (Block List, Min: 1, Max: 1) Authentication credentials to access the private OCI registry. Required if is_private is set to true (see below for nested schema)
                      Authentication credentials to access the private OCI registry. Required if `is_private` is set to `true`
                    items:
                      properties:
                        accessKey:
                          description: |-
                            (String) The access key for accessing the registry. Required if 'credential_type' is set to 'secret'.
                            The access key for accessing the registry. Required if 'credential_type' is set to 'secret'.
                          type: string
                        arn:
                          description: |-
                            based authentication. Required if 'credential_type' is 'sts'.
                            The Amazon Resource Name (ARN) used for AWS-based authentication. Required if 'credential_type' is 'sts'.
                          type: string
                        credentialType:
                          description: |-
                            (String) The type of authentication used for accessing the registry. Supported values are 'secret', 'sts', 'basic', and 'noAuth'.
                            The type of authentication used for accessing the registry. Supported values are 'secret', 'sts', 'basic', and 'noAuth'.
                          type: string
                        externalId:
                          description: |-
                            (String) The external ID used for AWS STS (Security Token Service) authentication. Required if 'credential_type' is 'sts'.
                            The external ID used for AWS STS (Security Token Service) authentication. Required if 'credential_type' is 'sts'.
                          type: string
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) The password for basic authentication. Required if 'credential_type' is 'basic'.
                            The password for basic authentication. Required if 'credential_type' is 'basic'.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        secretKeySecretRef:
                          description: |-
                            (String, Sensitive) The secret key for accessing the registry. Required if 'credential_type' is set to 'secret'.
                            The secret key for accessing the registry. Required if 'credential_type' is set to 'secret'.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        tlsConfig:
                          description: |-
                            (Block List, Max: 1) TLS configuration for the registry. (see below for nested schema)
                            TLS configuration for the registry.
                          items:
                            properties:
                              certificate:
                                description: |-
                                  (String) Specifies the TLS certificate used for secure communication. Required for enabling SSL/TLS encryption.
                                  Specifies the TLS certificate used for secure communication. Required for enabling SSL/TLS encryption.
                                type: string
                              insecureSkipVerify:
                                description: |-
                                  in-the-middle attacks. Only use this when connecting to registries with self-signed certificates in trusted networks.
                                  Disables TLS certificate verification when set to true. ⚠️ WARNING: Setting this to true disables SSL certificate verification and makes connections vulnerable to man-in-the-middle attacks. Only use this when connecting to registries with self-signed certificates in trusted networks.
                                type: boolean
                            type: object
                          type: array
                        username:
                          description: |-
                            (String) The username for basic authentication. Required if 'credential_type' is 'basic'.
                            The username for basic authentication. Required if 'credential_type' is 'basic'.
                          type: string
                      type: object
                    type: array
                  endpoint:
                    description: |-
                      (String) The URL endpoint of the OCI registry. This is where the container images are hosted and accessed.
                      The URL endpoint of the OCI registry. This is where the container images are hosted and accessed.
                    type: string
                  endpointSuffix:
                    description: |-
                      (String) Specifies a suffix to append to the endpoint. This field is optional, but some registries (e.g., JFrog) may require it. The final registry URL is constructed by appending this suffix to the endpoint.
                      Specifies a suffix to append to the endpoint. This field is optional, but some registries (e.g., JFrog) may require it. The final registry URL is constructed by appending this suffix to the endpoint.
                    type: string
                  isPrivate:
                    description: |-
                      (Boolean) Specifies whether the registry is private or public. Private registries require authentication to access.
                      Specifies whether the registry is private or public. Private registries require authentication to access.
                    type: boolean
                  isSynchronization:
                    description: |-
                      (Boolean) Specifies whether the registry is synchronized.
                      Specifies whether the registry is synchronized.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the OCI registry.
                      The name of the OCI registry.
                    type: string
                  providerType:
                    description: |-
                      (String) The type of provider used for interacting with the registry. Supported value's are helm, zarf and pack, The default is 'helm'. zarf is allowed with type="basic"
                      The type of provider used for interacting with the registry. Supported value's are `helm`, `zarf` and `pack`, The default is 'helm'. `zarf` is allowed with `type="basic"`
                    type: string
                  type:
                    description: |-
                      (String) The type of the registry. Possible values are 'ecr' (Amazon Elastic Container Registry) or 'basic' (for other types of OCI registries).
                      The type of the registry. Possible values are 'ecr' (Amazon Elastic Container Registry) or 'basic' (for other types of OCI registries).
                    type: string
                  waitForStatusMessage:
                    description: |-
                      (String) The status message from the last sync operation. This is a computed field that is populated after sync completes.
                      The status message from the last sync operation. This is a computed field that is populated after sync completes.
                    type: string
                  waitForSync:
                    description: This option is applicable when provider_type is set
                      to zarf or helm. Default value is false. This option is applicable
                      when `provider_type` is set to `zarf` or `helm`. Default value
                      is `false`.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  baseContentPath:
                    description: |-
                      (String) The relative path to the endpoint specified.
                      The relative path to the endpoint specified.
                    type: string
                  credentials:
                    description: |-
                      (Block List, Min: 1, Max: 1) Authentication credentials to access the private OCI registry. Required if is_private is set to true (see below for nested schema)
                      Authentication credentials to access the private OCI registry. Required if `is_private` is set to `true`
                    items:
                      properties:
                        accessKey:
                          description: |-
                            (String) The access key for accessing the registry. Required if 'credential_type' is set to 'secret'.
                            The access key for accessing the registry. Required if 'credential_type' is set to 'secret'.
                          type: string
                        arn:
                          description: |-
                            based authentication. Required if 'credential_type' is 'sts'.
                            The Amazon Resource Name (ARN) used for AWS-based authentication. Required if 'credential_type' is 'sts'.
                          type: string
                        credentialType:
                          description: |-
                            (String) The type of authentication used for accessing the registry. Supported values are 'secret', 'sts', 'basic', and 'noAuth'.
                            The type of authentication used for accessing the registry. Supported values are 'secret', 'sts', 'basic', and 'noAuth'.
                          type: string
                        externalId:
                          description: |-
                            (String) The external ID used for AWS STS (Security Token Service) authentication. Required if 'credential_type' is 'sts'.
                            The external ID used for AWS STS (Security Token Service) authentication. Required if 'credential_type' is 'sts'.
                          type: string
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) The password for basic authentication. Required if 'credential_type' is 'basic'.
                            The password for basic authentication. Required if 'credential_type' is 'basic'.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        secretKeySecretRef:
                          description: |-
                            (String, Sensitive) The secret key for accessing the registry. Required if 'credential_type' is set to 'secret'.
                            The secret key for accessing the registry. Required if 'credential_type' is set to 'secret'.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        tlsConfig:
                          description: |-
                            (Block List, Max: 1) TLS configuration for the registry. (see below for nested schema)
                            TLS configuration for the registry.
                          items:
                            properties:
                              certificate:
                                description: |-
                                  (String) Specifies the TLS certificate used for secure communication. Required for enabling SSL/TLS encryption.
                                  Specifies the TLS certificate used for secure communication. Required for enabling SSL/TLS encryption.
                                type: string
                              insecureSkipVerify:
                                description: |-
                                  in-the-middle attacks. Only use this when connecting to registries with self-signed certificates in trusted networks.
                                  Disables TLS certificate verification when set to true. ⚠️ WARNING: Setting this to true disables SSL certificate verification and makes connections vulnerable to man-in-the-middle attacks. Only use this when connecting to registries with self-signed certificates in trusted networks.
                                type: boolean
                            type: object
                          type: array
                        username:
                          description: |-
                            (String) The username for basic authentication. Required if 'credential_type' is 'basic'.
                            The username for basic authentication. Required if 'credential_type' is 'basic'.
                          type: string
                      type: object
                    type: array
                  endpoint:
                    description: |-
                      (String) The URL endpoint of the OCI registry. This is where the container images are hosted and accessed.
                      The URL endpoint of the OCI registry. This is where the container images are hosted and accessed.
                    type: string
                  endpointSuffix:
                    description: |-
                      (String) Specifies a suffix to append to the endpoint. This field is optional, but some registries (e.g., JFrog) may require it. The final registry URL is constructed by appending this suffix to the endpoint.
                      Specifies a suffix to append to the endpoint. This field is optional, but some registries (e.g., JFrog) may require it. The final registry URL is constructed by appending this suffix to the endpoint.
                    type: string
                  isPrivate:
                    description: |-
                      (Boolean) Specifies whether the registry is private or public. Private registries require authentication to access.
                      Specifies whether the registry is private or public. Private registries require authentication to access.
                    type: boolean
                  isSynchronization:
                    description: |-
                      (Boolean) Specifies whether the registry is synchronized.
                      Specifies whether the registry is synchronized.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the OCI registry.
                      The name of the OCI registry.
                    type: string
                  providerType:
                    description: |-
                      (String) The type of provider used for interacting with the registry. Supported value's are helm, zarf and pack, The default is 'helm'. zarf is allowed with type="basic"
                      The type of provider used for interacting with the registry. Supported value's are `helm`, `zarf` and `pack`, The default is 'helm'. `zarf` is allowed with `type="basic"`
                    type: string
                  type:
                    description: |-
                      (String) The type of the registry. Possible values are 'ecr' (Amazon Elastic Container Registry) or 'basic' (for other types of OCI registries).
                      The type of the registry. Possible values are 'ecr' (Amazon Elastic Container Registry) or 'basic' (for other types of OCI registries).
                    type: string
                  waitForStatusMessage:
                    description: |-
                      (String) The status message from the last sync operation. This is a computed field that is populated after sync completes.
                      The status message from the last sync operation. This is a computed field that is populated after sync completes.
                    type: string
                  waitForSync:
                    description: This option is applicable when provider_type is set
                      to zarf or helm. Default value is false. This option is applicable
                      when `provider_type` is set to `zarf` or `helm`. Default value
                      is `false`.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.credentials is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.credentials)
                || (has(self.initProvider) && has(self.initProvider.credentials))'
            - message: spec.forProvider.endpoint is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.endpoint)
                || (has(self.initProvider) && has(self.initProvider.endpoint))'
            - message: spec.forProvider.isPrivate is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.isPrivate)
                || (has(self.initProvider) && has(self.initProvider.isPrivate))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: OciStatus defines the observed state of Oci.
            properties:
              atProvider:
                properties:
                  baseContentPath:
                    description: |-
                      (String) The relative path to the endpoint specified.
                      The relative path to the endpoint specified.
                    type: string
                  credentials:
                    description: |-
                      (Block List, Min: 1, Max: 1) Authentication credentials to access the private OCI registry. Required if is_private is set to true (see below for nested schema)
                      Authentication credentials to access the private OCI registry. Required if `is_private` is set to `true`
                    items:
                      properties:
                        accessKey:
                          description: |-
                            (String) The access key for accessing the registry. Required if 'credential_type' is set to 'secret'.
                            The access key for accessing the registry. Required if 'credential_type' is set to 'secret'.
                          type: string
                        arn:
                          description: |-
                            based authentication. Required if 'credential_type' is 'sts'.
                            The Amazon Resource Name (ARN) used for AWS-based authentication. Required if 'credential_type' is 'sts'.
                          type: string
                        credentialType:
                          description: |-
                            (String) The type of authentication used for accessing the registry. Supported values are 'secret', 'sts', 'basic', and 'noAuth'.
                            The type of authentication used for accessing the registry. Supported values are 'secret', 'sts', 'basic', and 'noAuth'.
                          type: string
                        externalId:
                          description: |-
                            (String) The external ID used for AWS STS (Security Token Service) authentication. Required if 'credential_type' is 'sts'.
                            The external ID used for AWS STS (Security Token Service) authentication. Required if 'credential_type' is 'sts'.
                          type: string
                        tlsConfig:
                          description: |-
                            (Block List, Max: 1) TLS configuration for the registry. (see below for nested schema)
                            TLS configuration for the registry.
                          items:
                            properties:
                              certificate:
                                description: |-
                                  (String) Specifies the TLS certificate used for secure communication. Required for enabling SSL/TLS encryption.
                                  Specifies the TLS certificate used for secure communication. Required for enabling SSL/TLS encryption.
                                type: string
                              insecureSkipVerify:
                                description: |-
                                  in-the-middle attacks. Only use this when connecting to registries with self-signed certificates in trusted networks.
                                  Disables TLS certificate verification when set to true. ⚠️ WARNING: Setting this to true disables SSL certificate verification and makes connections vulnerable to man-in-the-middle attacks. Only use this when connecting to registries with self-signed certificates in trusted networks.
                                type: boolean
                            type: object
                          type: array
                        username:
                          description: |-
                            (String) The username for basic authentication. Required if 'credential_type' is 'basic'.
                            The username for basic authentication. Required if 'credential_type' is 'basic'.
                          type: string
                      type: object
                    type: array
                  endpoint:
                    description: |-
                      (String) The URL endpoint of the OCI registry. This is where the container images are hosted and accessed.
                      The URL endpoint of the OCI registry. This is where the container images are hosted and accessed.
                    type: string
                  endpointSuffix:
                    description: |-
                      (String) Specifies a suffix to append to the endpoint. This field is optional, but some registries (e.g., JFrog) may require it. The final registry URL is constructed by appending this suffix to the endpoint.
                      Specifies a suffix to append to the endpoint. This field is optional, but some registries (e.g., JFrog) may require it. The final registry URL is constructed by appending this suffix to the endpoint.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isPrivate:
                    description: |-
                      (Boolean) Specifies whether the registry is private or public. Private registries require authentication to access.
                      Specifies whether the registry is private or public. Private registries require authentication to access.
                    type: boolean
                  isSynchronization:
                    description: |-
                      (Boolean) Specifies whether the registry is synchronized.
                      Specifies whether the registry is synchronized.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the OCI registry.
                      The name of the OCI registry.
                    type: string
                  providerType:
                    description: |-
                      (String) The type of provider used for interacting with the registry. Supported value's are helm, zarf and pack, The default is 'helm'. zarf is allowed with type="basic"
                      The type of provider used for interacting with the registry. Supported value's are `helm`, `zarf` and `pack`, The default is 'helm'. `zarf` is allowed with `type="basic"`
                    type: string
                  type:
                    description: |-
                      (String) The type of the registry. Possible values are 'ecr' (Amazon Elastic Container Registry) or 'basic' (for other types of OCI registries).
                      The type of the registry. Possible values are 'ecr' (Amazon Elastic Container Registry) or 'basic' (for other types of OCI registries).
                    type: string
                  waitForStatusMessage:
                    description: |-
                      (String) The status message from the last sync operation. This is a computed field that is populated after sync completes.
                      The status message from the last sync operation. This is a computed field that is populated after sync completes.
                    type: string
                  waitForSync:
                    description: This option is applicable when provider_type is set
                      to zarf or helm. Default value is false. This option is applicable
                      when `provider_type` is set to `zarf` or `helm`. Default value
                      is `false`.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
